name: Mutation Testing

# Nightly mutation testing pass over the Rust crypto crate.
#
# Audit follow-up: this workflow previously ran `mutmut` against
# `protocol/hashes.py`, `protocol/merkle.py`, `protocol/ssmf.py`, and
# `protocol/canonical.py` — all retired with the Python FastAPI server in
# v0.9.0. The center of gravity is now `crates/olympus-crypto`, which is
# parity-locked against `light-poseidon` and consumed by the Tauri Axum
# backend. ADR-0002 acknowledges this migration.

on:
  workflow_dispatch:
  schedule:
    # Run nightly at 03:00 UTC — mutation testing is expensive
    - cron: "0 3 * * *"
  pull_request:
    paths:
      # Restrict PR-triggered mutation runs to files inside the two
      # mutation scopes plus this workflow itself. Unrelated PRs don't
      # pay the cost. Each job still runs on every PR matching this
      # union — gating per-job by which-path-actually-changed would
      # need a paths-filter action; keeping it simple here, the cache
      # hit on the unaffected job keeps it cheap in practice.
      - "crates/olympus-crypto/**"
      - "src-tauri/src/zk/prove.rs"
      - "src-tauri/src/zk/verify.rs"
      - "src-tauri/src/zk/zkey.rs"
      - "src-tauri/tests/zk*.rs"
      - "src-tauri/tests/zk_fixtures/**"
      - ".cargo/mutants.toml"
      - ".github/workflows/mutation-testing.yml"
      - ".github/actions/zk-ceremony-artifacts/**"

concurrency:
  group: mutation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  mutation-test-crypto:
    name: cargo-mutants — olympus-crypto
    runs-on: ubuntu-latest
    permissions:
      contents: read
    # Mutation testing builds + tests every mutation in series; for the
    # current mutant surface (lib.rs + poseidon.rs + canonical.rs +
    # smt.rs + ledger_snapshot.rs + trust_list.rs + container/{mod,pdf,
    # ooxml}.rs) on a `ubuntu-latest` runner this fits in 120 minutes.
    # Bump if the crate grows.
    # (Was 90 for the ~220-mutant surface before ADR-0041's trust_list.rs
    # added ~132 mutants. The container modules add ~56 more — measured from
    # this PR's `--in-diff` run, which mutated them in full as new files —
    # so the nightly/manual full pass is now ~406. That is still inside the
    # 120-minute budget on the observed per-mutant cost, but it is the
    # closest this has run to the cap; the next surface addition should come
    # with a measured full-pass duration rather than another estimate.)
    timeout-minutes: 120

    steps:
      # Keeps its credentials, deliberately: the diff-scoping step below runs
      # `git fetch --no-tags origin "$GITHUB_BASE_REF"`, which needs auth on a
      # private repo. This job cannot drop the token without losing the fetch
      # that bounds which mutants cargo-mutants runs.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Full history so the PR-diff step below can compute a merge-base
          # against the target branch for `--in-diff`.
          fetch-depth: 0

      - name: Scope mutation run to the PR diff (pull_request only)
        # On source-only PRs, mutate just the changed lines. Mutation-config or
        # workflow edits run the full campaign so a diff containing no mutable
        # Rust lines cannot produce a misleading zero-mutant green result.
        # Nightly/manual runs also do the full pass.
        if: github.event_name == 'pull_request'
        run: |
          set -euo pipefail
          git fetch --no-tags origin "${GITHUB_BASE_REF}"
          base_sha="$(git merge-base FETCH_HEAD HEAD)"
          git diff "${base_sha}" HEAD > pr.diff
          git diff --name-only "${base_sha}" HEAD > changed-files.txt
          echo "Scoping cargo-mutants to PR diff vs ${GITHUB_BASE_REF} (${base_sha}):"
          git --no-pager diff --stat "${base_sha}" HEAD || true
          if grep -Eq '^(\.cargo/mutants\.toml|\.github/workflows/mutation-testing\.yml)$' changed-files.txt; then
            echo "Mutation config changed; running the full olympus-crypto campaign."
          else
            echo "MUTANTS_IN_DIFF=--in-diff pr.diff" >> "$GITHUB_ENV"
          fi

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # stable
        with:
          toolchain: 1.95.0

      - name: Rust cache
        uses: Swatinem/rust-cache@f0d9c3887740aee45f6153b24b3a6b815192ec16 # v2
        with:
          workspaces: ". -> target"

      - name: Cache cargo-mutants binary
        id: cache-cargo-mutants
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ~/.cargo/bin/cargo-mutants
          # Cache key MUST match CARGO_MUTANTS_VERSION below so a cache
          # miss installs the same version that's stored under this key.
          key: cargo-mutants-27.0.0

      - name: Install cargo-mutants
        if: steps.cache-cargo-mutants.outputs.cache-hit != 'true'
        # Pin to the version implied by the cache key; an unpinned
        # `cargo install` could store a newer release under a key that
        # claims a specific version, drifting silently on subsequent
        # runs.
        run: cargo install cargo-mutants --locked --version 27.0.0

      - name: Run mutation testing (olympus-crypto)
        run: |
          # Enable every feature with tests so the per-mutation test command
          # is the same one a developer runs locally.
          # `--no-shuffle` keeps the run order deterministic so reruns of a
          # specific mutation can be reproduced from the same line numbers.
          # `--timeout 90` caps any single mutation's test run at 90 s
          # (default 60 s is borderline tight under CI runner CPU contention).
          #
          # cargo-mutants exit code contract (per its docs):
          #   0  — all mutants caught (or no mutants found)
          #   1  — usage error / internal error
          #   2  — one or more mutants survived (informational; not a hard
          #        regression — fix lands in a separate test PR)
          #   3  — one or more mutants timed out (triage signal; not an
          #        infrastructure failure if outcomes.json confirms timeouts)
          #   4  — baseline tests failed or hung before mutation testing
          #   5/6 — invalid or mismatched --in-diff input
          #   70 — internal error
          #
          # Treat only 0 as success. A surviving mutant (exit 2) is a failed
          # security gate. Treat 3 as advisory only when outcomes.json confirms
          # real mutant timeouts; other exits indicate broken infrastructure.
          set +e
          # ${MUTANTS_IN_DIFF} is `--in-diff pr.diff` on pull_request runs
          # (set by the "Scope mutation run to the PR diff" step) and empty
          # on scheduled/dispatch runs, which therefore do a full pass.
          cargo mutants \
            -p olympus-crypto \
            --features poseidon,smt,snapshot,canonical,redaction,signature-envelope,trust-list,trust-list-wire,container \
            --no-shuffle \
            --timeout 90 \
            ${MUTANTS_IN_DIFF:-}
          rc=$?
          set -e
          timed_out=0
          if [ -f mutants.out/outcomes.json ]; then
            timed_out=$(jq '[.outcomes[] | select(.scenario | type == "object" and has("Mutant")) | select(.summary == "Timeout")] | length' \
                         mutants.out/outcomes.json 2>/dev/null || echo 0)
          fi
          case "$rc" in
            0) echo "cargo mutants caught every viable olympus-crypto mutant" ;;
            2)
              echo "::error::cargo-mutants found surviving olympus-crypto mutants"
              exit 2
              ;;
            3)
              if [ "$timed_out" -gt 0 ]; then
                # Treat real cargo-mutants timeouts as advisory even when no
                # mutants completed; PR-diff runs can legitimately time out on
                # the first selected mutant under CI runner contention.
                echo "::warning::cargo mutants exit=3 with ${timed_out} timeout(s) — treating as advisory; inspect mutants-out artifact"
              else
                echo "cargo mutants exit=$rc — failing the job"; exit "$rc"
              fi
              ;;
            *)   echo "cargo mutants exit=$rc — failing the job"; exit "$rc" ;;
          esac

          # Catch the silent-pass failure mode where every mutant is unviable
          # (couldn't build). cargo-mutants exits 0 with a WARN; without this
          # guard the gate goes green while testing zero behaviour. Trigger
          # cases include workspace breakage during the per-mutant rebuild
          # (scratch-dir issue, missing system dep, broken feature combo).
          #
          # Source of truth is outcomes.json. The schema is one outcome per
          # mutant with a `summary` of "Success" | "CaughtMutant" |
          # "MissedMutant" | "Unviable" | "Timeout" | …. A viable mutant is
          # anything other than Unviable on the in-diff scope; if zero
          # viable mutants ran, the gate failed to validate anything.
          total=0
          viable=0
          completed=0
          timed_out=0
          if [ -f mutants.out/outcomes.json ]; then
            # Filter to actual Mutant scenarios — `.outcomes` also contains
            # the Baseline scenario (a "Success" outcome that's not a mutant
            # at all), and counting it as viable would mask the very failure
            # mode this guard exists to catch.
            mutants_jq='[.outcomes[] | select(.scenario | type == "object" and has("Mutant"))]'
            total=$(jq "${mutants_jq} | length" mutants.out/outcomes.json 2>/dev/null || echo 0)
            viable=$(jq "${mutants_jq} | map(select(.summary != \"Unviable\")) | length" \
                       mutants.out/outcomes.json 2>/dev/null || echo 0)
            completed=$(jq "${mutants_jq} | map(select(.summary != \"Unviable\" and .summary != \"Timeout\")) | length" \
                        mutants.out/outcomes.json 2>/dev/null || echo 0)
            timed_out=$(jq "${mutants_jq} | map(select(.summary == \"Timeout\")) | length" \
                         mutants.out/outcomes.json 2>/dev/null || echo 0)
            echo "outcomes: ${total} mutants, ${viable} viable (non-Unviable), ${completed} completed, ${timed_out} timeout(s)"
          fi

          case "$rc" in
            0) echo "cargo mutants caught every viable olympus-crypto mutant" ;;
            2)
              echo "::error::cargo-mutants found surviving olympus-crypto mutants"
              exit 2
              ;;
            3)
              if [ "$timed_out" -gt 0 ]; then
                echo "::warning::cargo-mutants timed out on ${timed_out} mutant(s) — see mutants-out artifact, mutants.out/outcomes.json, and mutants.out/log/*"
              else
                echo "cargo mutants exit=$rc — failing the job"
                exit "$rc"
              fi
              ;;
            *)   echo "cargo mutants exit=$rc — failing the job"; exit "$rc" ;;
          esac

          # olympus-crypto has no documented non-constructible return-type
          # surface (unlike the zk job's Proof<Bn254>), so all-unviable is
          # never legitimate here — the alarm stays unconditional, including
          # on --in-diff pull-request runs (an in-diff run selecting zero
          # mutants has total=0 and skips it).
          if [ "$total" -gt 0 ] && [ "$viable" -eq 0 ]; then
            echo "::error::cargo-mutants found ${total} mutants but ALL were unviable — gate validated nothing. Inspect mutants.out/log/* in the mutants-out artifact for the real cause. Common modes: (a) per-mutant build failure — scratch-dir, missing system dep, broken feature combo; (b) every return type non-constructible by cargo-mutants' default placeholders (E0599 on a default ctor / E0277 on a missing Default impl) — add an Err(...) replacement via .cargo/mutants.toml's error_values; (c) ZK artifacts not restored — see .github/actions/zk-ceremony-artifacts."
            exit 1
          fi

      - name: Summarise mutation results
        if: always()
        run: |
          echo "## Mutation Testing — olympus-crypto" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          if [ -f mutants.out/outcomes.json ]; then
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            jq -r '"total_mutants: \(.total_mutants)\ncaught: \(.caught)\nmissed: \(.missed)\ntimeout: \(.timeout)\nunviable: \(.unviable)\nsuccess: \(.success)"' \
              mutants.out/outcomes.json 2>/dev/null \
              >> "$GITHUB_STEP_SUMMARY" \
              || cat mutants.out/outcomes.json | head -50 >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
          else
            printf '%s\n' '_No `mutants.out/outcomes.json` produced — the run probably bailed early._' >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload mutants.out
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: mutants-out
          path: mutants.out/
          retention-days: 30
          if-no-files-found: warn

      - name: Flag surviving mutants
        if: always()
        run: |
          # Report survivors even when the mutation step has already failed,
          # so the artifact and job summary remain useful for test repair.
          if [ -f mutants.out/caught.txt ] && [ -f mutants.out/missed.txt ]; then
            caught=$(wc -l < mutants.out/caught.txt || echo 0)
            missed=$(wc -l < mutants.out/missed.txt || echo 0)
            timed_out=0
            if [ -f mutants.out/outcomes.json ]; then
              timed_out=$(jq '[.outcomes[] | select((.scenario | type == "object" and has("Mutant")) and .summary == "Timeout")] | length' mutants.out/outcomes.json 2>/dev/null || echo 0)
            fi
            echo "Caught: $caught   Missed: $missed   Timed out: $timed_out"
            if [ "$missed" -gt 0 ]; then
              echo "::warning::$missed mutant(s) survived — see mutants-out artifact, mutants.out/missed.txt"
            fi
            if [ "$timed_out" -gt 0 ]; then
              echo "::warning::$timed_out mutant(s) timed out — see mutants-out artifact, mutants.out/outcomes.json, and mutants.out/log/*"
            fi
          fi

  mutation-test-zk:
    name: cargo-mutants — src-tauri zk (${{ matrix.shard }}/8)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      # Reuse the checkout's target directory with `--in-place`; cargo-mutants'
      # default scratch copy excludes target/ and previously rebuilt the whole
      # desktop package for every mutant. mold + the dedicated profile further
      # reduce relink cost without changing normal test/release artifacts.
      CARGO_TARGET_DIR: ${{ github.workspace }}/target
      RUSTFLAGS: -C link-arg=-fuse-ld=mold
    strategy:
      fail-fast: false
      # main.rs and lib.rs intentionally include the same desktop module tree.
      # The run step stubs main.rs while discovering mutants, leaving 119 unique
      # ZK mutants instead of testing the same source surface twice.
      # Eight slices cap each worker at 15 mutants.
      matrix:
        # cargo-mutants requires zero-based k/n shard indices (k < n).
        shard: [0, 1, 2, 3, 4, 5, 6, 7]
    timeout-minutes: 180

    steps:
      # Same as the crypto shard above: keeps its credentials because the
      # diff-scoping `git fetch` needs auth on a private repo.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Full history so the PR-diff step below can compute a merge-base
          # against the target branch for `--in-diff`.
          fetch-depth: 0

      - name: Scope mutation run to the PR diff (pull_request only)
        # On source-only PRs, mutate just the changed lines. Test, fixture, or
        # mutation-infrastructure edits run the full targeted campaign so the
        # new tests are measured against real source mutants rather than an
        # empty test-file diff. Nightly/manual runs also do the full pass.
        if: github.event_name == 'pull_request'
        run: |
          set -euo pipefail
          git fetch --no-tags origin "${GITHUB_BASE_REF}"
          base_sha="$(git merge-base FETCH_HEAD HEAD)"
          git diff "${base_sha}" HEAD > pr.diff
          git diff --name-only "${base_sha}" HEAD > changed-files.txt
          echo "Scoping cargo-mutants to PR diff vs ${GITHUB_BASE_REF} (${base_sha}):"
          git --no-pager diff --stat "${base_sha}" HEAD || true
          if grep -Eq '^(src-tauri/tests/zk.*\.rs|src-tauri/tests/zk_fixtures/|\.cargo/mutants\.toml|\.github/workflows/mutation-testing\.yml|\.github/actions/zk-ceremony-artifacts/)' changed-files.txt; then
            echo "ZK tests, fixtures, or mutation infrastructure changed; running the full targeted source campaign."
          else
            echo "MUTANTS_IN_DIFF=--in-diff pr.diff" >> "$GITHUB_ENV"
          fi

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # stable
        with:
          toolchain: 1.95.0

      - name: Install Linux system deps for src-tauri
        # src-tauri depends on libsoup/javascriptcoregtk/webkit2gtk for the
        # Tauri webview build, plus pkg-config to resolve them. The
        # mutation runs `cargo build`/`cargo test` per mutant, both of
        # which need these to link successfully.
        #
        # See ci.yml for the apt-hardening rationale (step-level timeout +
        # per-invocation timeout + retry loop).
        timeout-minutes: 20
        env:
          DEBIAN_FRONTEND: noninteractive
          APT_LISTCHANGES_FRONTEND: none
        run: |
          set -euo pipefail
          for i in 1 2 3; do
            if sudo -E timeout 240 apt-get update -o Acquire::Retries=3; then
              break
            fi
            echo "apt-get update attempt $i failed; sleeping 5 s"
            sleep 5
            if [ "$i" = "3" ]; then
              echo "apt-get update failed 3 times — aborting"
              exit 1
            fi
          done
          sudo -E timeout 600 apt-get install -y \
            mold pkg-config libssl-dev \
            libgtk-3-dev libsoup-3.0-dev libjavascriptcoregtk-4.1-dev \
            libwebkit2gtk-4.1-dev

      - name: Rust cache
        uses: Swatinem/rust-cache@f0d9c3887740aee45f6153b24b3a6b815192ec16 # v2
        with:
          workspaces: ". -> target"
          shared-key: zk-mutation-profile

      - name: Cache cargo-mutants binary
        id: cache-cargo-mutants
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ~/.cargo/bin/cargo-mutants
          key: cargo-mutants-27.0.0

      - name: Install cargo-mutants
        if: steps.cache-cargo-mutants.outputs.cache-hit != 'true'
        run: cargo install cargo-mutants --locked --version 27.0.0

      - name: Restore (or build) ZK ceremony artifacts
        # Each per-mutant test run executes the zk_prove_*.rs integration
        # tests, which load proofs/build/<circuit>_final.ark.zkey at
        # runtime. Without this step they `eprintln!("[skip] artifacts
        # missing")` and return Ok — silently turning every mutation into
        # a false negative. The composite action restores from cache when
        # possible, falling back to a full ptau20 ceremony rebuild.
        uses: ./.github/actions/zk-ceremony-artifacts

      - name: Run mutation testing (src-tauri zk module)
        run: |
          # Mutate the three security-critical proving/verification files, but
          # compile and run only the library plus the five integration targets
          # that exercise them. Unrelated DB/HTTP/SMT/property targets stay in
          # normal CI and no longer multiply every mutant cycle.
          #
          # IMPORTANT — cargo-mutants does NOT mutate generic type
          # arguments. The class of bug fixed in #1011 — `Groth16::<E>`
          # silently defaulting to `LibsnarkReduction` instead of
          # `CircomReduction` — is structurally invisible to this tool.
          # The clippy `disallowed_methods` guard in `clippy.toml` is
          # what catches that bug class; this mutation job complements
          # it by catching body-level / operator-level regressions in
          # the prover surface (function bodies replaced with default
          # returns, boolean negations, arithmetic operator swaps).
          # IMPORTANT — the `quorum-circuit` feature MUST be enabled here.
          # `prove_quorum` is gated behind it; with only `--features prover`
          # the function is cfg-excluded, so a mutation of its body still
          # builds + tests green and cargo-mutants reports a meaningless
          # MISSED "survivor" against code that was never compiled. Its
          # *artifact-dependent* prove path stays unkillable until the
          # federation_quorum trusted-setup ceremony produces a real
          # `.ark.zkey` (same status as the unified circuit) — the
          # whole-body mutant is killed by an invalid-witness unit test that
          # exercises the native pre-check without artifacts.
          set +e
          # ${MUTANTS_IN_DIFF} is `--in-diff pr.diff` on pull_request runs
          # (intersected with the --file scope below) and empty on
          # scheduled/dispatch runs, which do the full prove.rs pass.
          # Scope: `prove.rs` is the file the #1011 fix landed on, but
          # cargo-mutants 27 can only synthesize `Ok(<default-T>)` body
          # replacements and `ark_groth16::Proof<Bn254>` is not
          # Default-constructible — so prove.rs in isolation generates 100%
          # Unviable mutants. `.cargo/mutants.toml`'s `error_values` adds an
          # `Err(...)` mutant per Result-returning fn to recover viable
          # coverage of prove.rs itself; we also broaden the file scope to
          # `verify.rs` / `zkey.rs` whose return types ARE default-
          # constructible, so the run has a strong viable baseline even if
          # `error_values` ever drifts.
          # The selected real-circuit baseline takes several minutes on
          # hosted runners. A 600-second test timeout leaves contention
          # headroom while still bounding a mutant-induced hang.
          #
          # Both src/main.rs and src/lib.rs declare the desktop module tree.
          # cargo-mutants follows both top-level targets and otherwise discovers
          # every shared ZK mutation twice. The selected test targets exercise
          # the library, so use a disposable binary stub during discovery and
          # restore the real entry point even when cargo-mutants exits nonzero.
          desktop_main_backup="$(mktemp)"
          cp src-tauri/src/main.rs "$desktop_main_backup"
          restore_desktop_main() {
            cp "$desktop_main_backup" src-tauri/src/main.rs
            rm -f "$desktop_main_backup"
          }
          trap restore_desktop_main EXIT
          printf '%s\n' 'fn main() {}' > src-tauri/src/main.rs

          cargo mutants \
            -p olympus-desktop \
            --file src-tauri/src/zk/prove.rs \
            --file src-tauri/src/zk/verify.rs \
            --file src-tauri/src/zk/zkey.rs \
            --no-default-features \
            --features prover,quorum-circuit,zk-test-utils \
            --profile mutants \
            --in-place \
            --baseline run \
            --no-shuffle \
            --shard "${{ matrix.shard }}/8" \
            --sharding slice \
            --build-timeout 1200 \
            --timeout 600 \
            --cargo-arg=--lib \
            --cargo-arg=--test=zk_prove_existence \
            --cargo-arg=--test=zk_prove_non_existence \
            --cargo-arg=--test=zk_prove_unified \
            --cargo-arg=--test=zk_soundness \
            --cargo-arg=--test=zk_soundness_false_statement \
            ${MUTANTS_IN_DIFF:-}
          rc=$?
          set -e
          timed_out=0
          if [ -f mutants.out/outcomes.json ]; then
            timed_out=$(jq '[.outcomes[] | select(.scenario | type == "object" and has("Mutant")) | select(.summary == "Timeout")] | length' \
                         mutants.out/outcomes.json 2>/dev/null || echo 0)
          fi
          case "$rc" in
            0|2|3|4) ;; # Parse and report expected cargo-mutants outcomes below.
            *)   echo "cargo mutants exit=$rc — failing the job"; exit "$rc" ;;
          esac

          # Catch the silent-pass failure mode where every mutant is
          # unviable. The zk job is especially prone to this — per-mutant
          # rebuilds load `.ark.zkey` artifacts and link against system
          # webkit/gtk, so a missing dep, an interrupted ceremony-artifact
          # restore, or a feature-flag drift can turn all N mutants into
          # build failures while cargo-mutants still exits 0. See the
          # equivalent guard in the crypto job above.
          total=0
          viable=0
          completed=0
          timed_out=0
          if [ -f mutants.out/outcomes.json ]; then
            # Filter to actual Mutant scenarios — `.outcomes` also contains
            # the Baseline scenario (a "Success" outcome that's not a mutant
            # at all), and counting it as viable would mask the very failure
            # mode this guard exists to catch.
            mutants_jq='[.outcomes[] | select(.scenario | type == "object" and has("Mutant"))]'
            total=$(jq "${mutants_jq} | length" mutants.out/outcomes.json 2>/dev/null || echo 0)
            viable=$(jq "${mutants_jq} | map(select(.summary != \"Unviable\")) | length" \
                       mutants.out/outcomes.json 2>/dev/null || echo 0)
            completed=$(jq "${mutants_jq} | map(select(.summary != \"Unviable\" and .summary != \"Timeout\")) | length" \
                        mutants.out/outcomes.json 2>/dev/null || echo 0)
            timed_out=$(jq "${mutants_jq} | map(select(.summary == \"Timeout\")) | length" \
                         mutants.out/outcomes.json 2>/dev/null || echo 0)
            echo "outcomes: ${total} mutants, ${viable} viable (non-Unviable), ${completed} completed, ${timed_out} timeout(s)"
          fi

          case "$rc" in
            0) echo "cargo mutants caught every viable mutant in shard ${{ matrix.shard }}/8" ;;
            2)
              echo "::error::cargo-mutants found surviving mutants in shard ${{ matrix.shard }}/8"
              exit 2
              ;;
            3)
              echo "::error::cargo-mutants timed out on ${timed_out} mutant(s) in shard ${{ matrix.shard }}/8"
              exit 3
              ;;
            4)
              echo "::error::cargo-mutants baseline failed or timed out in shard ${{ matrix.shard }}/8"
              exit 4
              ;;
            *)   echo "cargo mutants exit=$rc — failing the job"; exit "$rc" ;;
          esac

          # All-unviable handling. On `--in-diff` pull-request runs a shard can
          # legitimately receive only unviable mutants: a small diff touching
          # only `Proof`-returning fn bodies in prove.rs generates the
          # non-constructible `Ok(...)` family (documented in
          # .cargo/mutants.toml) while the viable `Err(...)` siblings land in
          # other shards. That is the ONLY tolerated case, and only on in-diff
          # runs: it is allowlisted by file below, so an unviable mutant in
          # verify.rs / zkey.rs (whose return types ARE default-constructible)
          # or any broader build/dependency failure still fails closed. Full
          # (scheduled/dispatch) runs never tolerate all-unviable.
          # A tolerated unviable mutant must match ALL of: (1) file is
          # prove.rs, (2) the replacement is the Ok(...) genre (the Err(...)
          # siblings from error_values are expected to be viable — an unviable
          # Err mutant means the build environment itself is broken), and
          # (3) its per-mutant build log carries the documented
          # non-constructibility signature (E0599 missing ctor / E0283
          # inference failure). A missing artifact or dependency failure
          # produces different diagnostics and therefore fails closed even
          # when confined to prove.rs.
          unexplained_unviable=$(jq "${mutants_jq} | map(select(.summary == \"Unviable\" and (((.scenario.Mutant.file // \"\") != \"src-tauri/src/zk/prove.rs\") or (((.scenario.Mutant.replacement // \"\") | startswith(\"Ok(\")) | not)))) | length" \
                       mutants.out/outcomes.json 2>/dev/null || echo 0)
          # Count the allowlist candidates, then require every one of them to
          # be signature-verified through its build log. Comparing counts (not
          # looping over whatever log paths exist) means a candidate with a
          # missing or empty log_path field is unexplained by construction —
          # it can never be verified, so it fails the shard.
          allowlisted_jq="${mutants_jq} | map(select(.summary == \"Unviable\" and ((.scenario.Mutant.file // \"\") == \"src-tauri/src/zk/prove.rs\") and ((.scenario.Mutant.replacement // \"\") | startswith(\"Ok(\"))))"
          allowlisted=$(jq "${allowlisted_jq} | length" mutants.out/outcomes.json 2>/dev/null || echo 0)
          verified=0
          while IFS= read -r log_path; do
            [ -n "$log_path" ] || continue
            if grep -qE 'E0599|E0283' "mutants.out/${log_path}" 2>/dev/null; then
              verified=$((verified + 1))
            else
              echo "unviable prove.rs Ok(...) mutant log ${log_path} lacks the E0599/E0283 non-constructibility signature"
            fi
          done < <(jq -r "${allowlisted_jq} | .[].log_path // empty" \
                       mutants.out/outcomes.json 2>/dev/null || true)
          if [ "$verified" -lt "$allowlisted" ]; then
            echo "$((allowlisted - verified)) of ${allowlisted} allowlist-candidate mutant(s) could not be signature-verified (missing log_path, missing log file, or wrong diagnostics) — counting as unexplained"
            unexplained_unviable=$((unexplained_unviable + allowlisted - verified))
          fi
          if [ "$total" -gt 0 ] && [ "$viable" -eq 0 ]; then
            if [ -n "${MUTANTS_IN_DIFF:-}" ] && [ "$unexplained_unviable" -eq 0 ]; then
              echo "all ${total} in-diff mutants in shard ${{ matrix.shard }}/8 are the documented non-constructible Ok(Proof<Bn254>) family in prove.rs, each with the E0599/E0283 signature in its build log — tolerated; their viable Err(...) siblings are tested by other shards"
            else
            echo "::error::cargo-mutants found ${total} mutants but ALL were unviable (${unexplained_unviable} outside the prove.rs allowlist) — gate validated nothing. Inspect mutants.out/log/* in the mutants-out-zk-${{ matrix.shard }} artifact for the real cause. Common modes: (a) ZK ceremony artifacts not restored — every per-mutant 'cargo test' skips; (b) per-mutant build failure — scratch-dir, missing system dep (webkit/gtk), broken feature combo; (c) every return type non-constructible by cargo-mutants' default placeholders (E0599 on a default ctor / E0283 inference failure on 'Proof::from(Default::default())') — extend .cargo/mutants.toml's error_values. The zk job has hit (c) before because ark_groth16::Proof<Bn254> has no Default / new()."
              exit 1
            fi
          fi

      - name: Summarise mutation results
        if: always()
        run: |
          echo "## Mutation Testing — src-tauri zk shard ${{ matrix.shard }}/8" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          if [ -f mutants.out/outcomes.json ]; then
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            jq -r '"total_mutants: \(.total_mutants)\ncaught: \(.caught)\nmissed: \(.missed)\ntimeout: \(.timeout)\nunviable: \(.unviable)\nsuccess: \(.success)"' \
              mutants.out/outcomes.json 2>/dev/null \
              >> "$GITHUB_STEP_SUMMARY" \
              || cat mutants.out/outcomes.json | head -50 >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
          else
            printf '%s\n' '_No `mutants.out/outcomes.json` produced — the run probably bailed early._' >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload mutants.out
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: mutants-out-zk-${{ matrix.shard }}
          path: mutants.out/
          retention-days: 30
          if-no-files-found: warn

      - name: Flag surviving mutants
        if: always()
        run: |
          if [ -f mutants.out/caught.txt ] && [ -f mutants.out/missed.txt ]; then
            caught=$(wc -l < mutants.out/caught.txt || echo 0)
            missed=$(wc -l < mutants.out/missed.txt || echo 0)
            timed_out=0
            if [ -f mutants.out/outcomes.json ]; then
              timed_out=$(jq '[.outcomes[] | select((.scenario | type == "object" and has("Mutant")) and .summary == "Timeout")] | length' mutants.out/outcomes.json 2>/dev/null || echo 0)
            fi
            echo "Caught: $caught   Missed: $missed   Timed out: $timed_out"
            if [ "$missed" -gt 0 ]; then
              echo "::warning::$missed mutant(s) survived in src-tauri zk — see mutants-out-zk-${{ matrix.shard }} artifact"
            fi
            if [ "$timed_out" -gt 0 ]; then
              echo "::warning::$timed_out mutant(s) timed out in src-tauri zk — see mutants-out-zk-${{ matrix.shard }} artifact, mutants.out/outcomes.json, and mutants.out/log/*"
            fi
          fi
